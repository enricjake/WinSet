"""
GUI package for WinSet
======================

Contains all user-interface components built with CustomTkinter. This package
implements the visual layer of WinSet, including the main application window,
settings panels, profile management dialogs, tweak cards, and theming.

Modules within this package handle:
    - Main application window layout and navigation
    - Setting/tweak display and interactive editing widgets
    - Profile creation, selection, import/export dialogs
    - Preset browsing and one-click application
    - Toast notifications and progress indicators
    - Theme management (dark/light mode, accent colors)

Note: This package does not export symbols at the package level; individual
modules should be imported directly (e.g. ``from src.gui.app import App``).
"""
